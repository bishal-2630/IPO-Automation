name: IPO RESULT CHECK

on:
  schedule:
    - cron: "45 12 * * *" # 6:30 PM NPT
    - cron: "45 19 * * *" # 1:30 AM NPT
    - cron: "45 2 * * *"  # 8:30 AM NPT
  workflow_dispatch:

jobs:
  check-results:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run IPO Result Check
        shell: powershell
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          ENCRYPTION_KEY: ${{ secrets.ENCRYPTION_KEY }}
          FIREBASE_CREDENTIALS_B64: ${{ secrets.FIREBASE_CREDENTIALS_B64 }}
          PROXY_URL: ${{ secrets.PROXY_URL }}
          RUN_MODE: "check_status"
          HEADLESS: "false"
        run: |
          $env:PYTHONHOME=""
          $env:PYTHONPATH=""
          $env:PYTHONIOENCODING="utf-8"
          & "C:\Users\Bishal\AppData\Local\Programs\Python\Python312\python.exe" -u ipo_result.py
